{
  "name": "sid-web-projects",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "tailwindcss -i ./src/input.css -o ./public/output.css --watch",
    "build-js": "npx esbuild src/script.js --outfile=public/sout.js --bundle --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.0"
  },
  "dependencies": {
    "alpinejs": "^3.12.0",
    "esbuild": "^0.17.12",
    "swiper": "^9.1.1"
  }
}
